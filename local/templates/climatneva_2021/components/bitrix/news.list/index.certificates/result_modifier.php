<?if(!defined("B_PROLOG_INCLUDED") || B_PROLOG_INCLUDED!==true)die();

/* foreach ($arResult["ITEMS"] as &$item) {
    if (!empty($item["PREVIEW_PICTURE"]["ID"])) {
        $item["THUMB"] = CFile::ResizeImageGet($item["PREVIEW_PICTURE"]["ID"], ["width" => 350, "height" => 500])["src"];
    }
} */